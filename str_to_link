function string_to_link($string)
{
  $delete = array('.', ',', '"', '\'', '/', '!', ':', '?', '$', '&', '%');
	foreach ($delete as $del)
	{
		$string = str_replace($del, '', $string);
	}
	$link = str_replace(' ', '-', strtolower(ruslat($string)));
	return $link;
}
